The MySQL CA database is now a valid option in both CAJson.db and CAConfig.db type unions (sqlite|postgres|mysql), but the 'CA DB - MySQL' row still marks it unsupported (✕) for both Fabric v2 and v3. That row is stale and should reflect support consistent with SQLite/Postgres. Other diff changes (v2.6.0 bump, Fablo REST + dev mode validation, V3 Consortium guard, explorer YAML indentation fix, k8s explorer version removal, orderer TLS cert path) are bug fixes/internal changes that do not alter the feature matrix.